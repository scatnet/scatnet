x = uiuc_sample;
x = x(1

size_in = size(x);
filters = gabor_filter_bank_2d(size_in);
downsampler = @(j)(j);

out = wavelet_modulus_2d(x, filters, downsampler);

bigimg = display_gscatt_all(out,1);
imagesc(bigimg);
colormap gray;